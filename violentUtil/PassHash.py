import crypt

def getHashedPass(rawpass, saltstr):
    """
    return the hashed value with crypt password, but it's not support bcrypt.
    If bcrypt use getHashedPassWithBcrypt function.
    """
    pass


def getHashedPassWithBcrypt(rawpass, saltstr=NONE):
    pass
